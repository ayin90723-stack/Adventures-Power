# 冒险的力量 - 30 种能力实现详解

> **Adventure's Power** v1.3.1 · Minecraft Forge 1.20.1 · 开发者向文档
>
> 本文详细介绍模组全部 30 种能力的游戏效果与代码实现方式，面向熟悉 Java / Forge 开发的读者。所有类名、方法名、配置字段均与源码一致（包名 `com.ayin90723.adventure_power`）。数值均为默认配置，全部可在 TOML 配置中调整。

---

## 目录

1. [总体架构](#总体架构)
2. [共享基础设施](#共享基础设施)
3. [里程碑 1 · 初次夜冕](#里程碑-1--初次夜冕)：灵巧 / 大地之力
4. [里程碑 2 · 初尝败绩](#里程碑-2--初尝败绩)：虚空踏步 / 休养生息
5. [里程碑 3 · 初次交易](#里程碑-3--初次交易)：灵魂绑定 / 不动如山 / 加速
6. [里程碑 4 · 初探地底](#里程碑-4--初探地底)：全视之眼 / 无形之手 / 磁吸
7. [里程碑 5 · 初次附魔](#里程碑-5--初次附魔)：不朽装备 / 鸿运当头 / 经验加成
8. [里程碑 6 · 炽热之门](#里程碑-6--炽热之门)：环境免疫 / 嗜血 / 伤害抗性
9. [里程碑 7 · 凋零之陨](#里程碑-7--凋零之陨)：禁疗之触 / 坚韧之躯 / 死亡抗拒
10. [里程碑 8 · 幽匿之惧](#里程碑-8--幽匿之惧)：受击坚韧 / 净魂 / 满载而归
11. [里程碑 9 · 终末之翼](#里程碑-9--终末之翼)：翱翔 / 淬魂之力 / 破敌之眼 / 恩赐永驻
12. [里程碑 10 · 苍穹之证](#里程碑-10--苍穹之证)：影杀 / 真实血量 / 拒绝篡改 / 旅者之力
13. [附录：能力 × 实现层速查](#附录能力--实现层速查)
14. [附录：Mixin 注入点速查](#附录mixin-注入点速查)
15. [附录：版本演进摘要](#附录版本演进摘要)
16. [附录：能力联动关系](#附录能力联动关系)
17. [附录：验证与调试指引](#附录验证与调试指引)
18. [附录：文档维护约定](#附录文档维护约定)

---

## 总体架构

### 三层实现模型

30 种能力按对抗强度分布在三个实现层，**优先使用高层，仅在高层无法覆盖时才下沉**：

| 层 | 手段 | 特点 | 典型能力 |
|----|------|------|----------|
| **事件层** | Forge 事件（`LivingHurtEvent`、`PlayerTickEvent` 等） | 兼容性最好，其他模组可感知 | 灵巧、大地之力、伤害抗性 |
| **Mixin 层** | 字节码注入原版方法（`hurt`、`setHealth`、`canBeAffected` 等） | 拦截事件系统覆盖不到的路径 | 净魂、不朽装备、死亡抗拒 |
| **反射直写层** | `HealthUtil` 直接读写 `SynchedEntityData` 内部字段、实体追踪结构 | 对抗其他模组的 Mixin/ASM 拦截，"最终手段" | 影杀、真实血量、破敌之眼兜底 |

设计约定是**事件驱动优先**：能力的业务逻辑尽量放在事件层 Handler 中，Mixin 只做最低层拦截，反射直写只作为被外部模组拦截后的兜底补刀。

### 生效门禁模型

每个能力的效果代码执行前都要通过统一的门禁链：

```
服务端检查（isClientSide 直接 return）
  -> isAdventurer() 或 isFullyUnlocked()   佩戴冒险饰品激活的身份
  -> isAbilityEnabled("<id>")              玩家可在能力面板（P 键）自由开关
  -> （部分能力）isFullyUnlocked()          觉醒专属门禁
```

> **v1.2.7 重构**：门禁链统一封装到 `AbilityGate.isAbilityActive(player, id)`，需要 progress 对象时用 `getActiveProgress(player, id)`（避免 `isAbilityActive` + 再查一次的双重查询）。各 Handler 不再重复写 `getAdventureProgress -> isAdventurer||isFullyUnlocked -> isAbilityEnabled` 三连。百分比觉醒倍率统一走 `AbilityGate.awakenedRatio`（value/100，cap 0.95）/ `awakenedPercent`（不除 100，cap 参数）。
>
> **v1.3.1 补充**：① `AbilityGate.isActive(progress, id)` 一行断言收敛「冒险者/觉醒 + 能力启用」双行样板；`AbilityGate.value(progress, id)` 返回 `Optional<Float>`（能力未注册 = empty，与「null 不处理」语义完全等价）；② 能力 ID 字面量统一收敛到 `util/AbilityIds` 常量类（typo 编译期可查）；③ 高频 Mixin 注入点（`getHealth` / `getAttribute` / `setHealth`）经 `util/ProgressCache` 按 tick 缓存 progress 引用（弱 key 防泄漏；缓存的是引用而非门禁结果，能力开关/解锁/觉醒同 tick 即时生效，无延迟）。

**门禁与觉醒的关系（v1.2.1 修正）**：所有能力**统一遵循里程碑制**--所属里程碑解锁后即可使用，`isAbilityEnabled(id)` 内部会做里程碑归属校验。以下能力曾遗留 `isFullyUnlocked()` 硬门禁（未觉醒即使已解锁也不生效），v1.2.1 已统一改为 `isAdventurer() || isFullyUnlocked()` 组合门禁：

- 受击坚韧 `resilience`（里程碑 8 解锁）
- 死亡抗拒 `death_defy`（v1.3.0 起归属里程碑 7）
- 真实血量 `true_health` / 拒绝篡改 `reject_manip` / 旅者之力 `active_skill`（里程碑 10 解锁）

觉醒（`isFullyUnlocked()`）不再是这些能力的**准入门禁**，仅作为数值/机制强化层--例如受击坚韧觉醒 +6 层上限、死亡抗拒觉醒时联动免费审判、拒绝篡改觉醒 30% 反弹等，都在各 Handler 现有门禁通过后追加。

**v1.2.5 门禁两处加固**：
- **`adventurer` 不变式**：`activateFullyUnlocked()` 现连带置 `adventurer = true`（全解锁蕴含冒险者）；`checkAndActivateAdventurer()` 检测到 `fullyUnlocked = true` 但 `adventurer = false`（版本飞升 / 旧存档数据不一致）时自动补激活。由此 `isFullyUnlocked()` 为 true 时 `isAdventurer()` 必然也为 true，组合门禁 `!isAdventurer() && !isFullyUnlocked()` 两分支互为兜底。
- **攻击方能力 `receiveCanceled = true`**：淬魂 / 影杀 / 禁疗 / 破敌觉醒等攻击方能力的事件监听改用 `@SubscribeEvent(receiveCanceled = true)`，**不再检查 `event.isCanceled()`**--当亚波伦等模组在 `LivingHurtEvent` 取消伤害时，这些能力仍正常执行。仅受伤方能力（伤害抗性）保留 `isCanceled()` 门禁。同时新增 `rawAttacker` 回溯：`source.getEntity()` 非 Player 时依次尝试 `getDirectEntity()`、投射物 owner，兼容拔刀剑 / 投射物 `causingEntity` 缺失的场景。

### 数值成长公式

有成长的能力，其数值随**已解锁里程碑数**（`count`）线性或步进增长，`countAtUnlock` 为该能力所属里程碑的序号（由 `MilestoneRegistry` 从 milestones.json 加载后动态填充）：

```
线性：value = BASE + PER_MILESTONE × (count - countAtUnlock)      // 灵巧、伤害抗性、加速等
步进：value = BASE + STEP × ((count - countAtUnlock) / 2)         // 鸿运当头、休养生息等（每 2 里程碑一档）
固定：value = -1                                                   // 解锁即完整（灵魂绑定、净魂、不朽装备等）
```

> **v1.2.7 重构**：成长公式封装到基类--线性能力继承 `LinearGrowthAbility`（实现 `base()` / `perMilestone()` 从 `ModConfig` 取值）、步进能力继承 `StepGrowthAbility`（实现 `base()` / `step()`，默认 divisor=2）、无成长能力继承 `AbstractAbility` 用默认 `value() = -1`。`countAtUnlock` 字段与 setter 由 `AbstractAbility` 统一封装，子类构造传初始值（兜底，实际由 `MilestoneRegistry` 加载 JSON 后覆盖）。
>
> **v1.3.1 下限保护**：公式中 `count - countAtUnlock` 统一 `Math.max(0, ...)`——设计允许任意顺序解锁，跳序解锁时差值为负会产生反向效果（如坚韧之躯裁剪最大生命、鸿运当头扣附魔等级）。顺序解锁数值完全不变。

各能力成长类型与 `countAtUnlock`（= 所属里程碑序号，由 `MilestoneRegistry` 动态填充）一览：

| 成长类型 | 能力（countAtUnlock） |
|----------|----------------------|
| **线性**（`LinearGrowthAbility`） | 灵巧(1)、大地之力(1)、不动如山(3)、加速(3)、无形之手(4)、磁吸(4)、经验加成(5)、嗜血(6)、伤害抗性(6)、禁疗之触(7)、坚韧之躯(7) |
| **步进**（`StepGrowthAbility`，divisor=2，每 2 里程碑一档） | 休养生息(2)、鸿运当头(5) |
| **无成长**（`AbstractAbility`，value 固定 -1） | 虚空踏步(2)、灵魂绑定(3)、全视之眼(4)、不朽装备(5)、环境免疫(6)、死亡抗拒(7)、净魂(8)、满载而归(8)、翱翔(9)、淬魂之力(9)、破敌之眼(9)、恩赐永驻(9)、影杀(10)、真实血量(10)、拒绝篡改(10)、旅者之力(10) |
| **特殊** | 受击坚韧(8)：自实现 `value(int count)` 三档 5/8/12 层（配置 `resilience_stacks_8/9/10`），另有觉醒重载 `value(count, awakened)` |

> 注：禁疗之触/坚韧之躯虽为线性基类，但数值直接进效果时长/属性，不经过成长公式以外的转换；坚韧之躯觉醒用 `Math.ceil` 而非浮点乘法（见下文）。

### 觉醒机制（第 11 阶段）

全部里程碑达成并持有「冒险的终点」后 `isFullyUnlocked()` 为 true，各 Handler 在门禁后追加觉醒分支：

- **数值强化**：数值 × `AWAKEN_MULTIPLIER`（默认 1.5），部分带硬上限钳制（`awaken_percent_cap`，默认 0.95）
- **机制质变**：新增行为（御风冲刺、AOE 斩杀、免费审判、过量吸血转护盾、威胁雷达、御风联动移速等），使用各自独立的觉醒配置项
- 全部 30 种能力在觉醒阶段均获得强化（机制质变或数值强化）

觉醒强化类型速查（30 种能力全部有觉醒）：

| 类型 | 能力 |
|------|------|
| **×`AWAKEN_MULTIPLIER`（1.5）** | 灵巧（钳 0.95）、大地之力（另 + 无视水中/空中惩罚）、不动如山（钳 100%）、无形之手、经验加成（×`awaken_xp_boost_mult`）、伤害抗性（钳 0.95）、加速（移速部分，另 + 疾跑推开） |
| **独立配置数值** | 休养生息（+5 HP）、磁吸（半径 ×1.5 + 吸经验球）、不朽装备（护甲 +2 / 武器 +25%）、鸿运当头（+2 等级）、禁疗之触（+ 易伤 ×1.2）、坚韧之躯（×1.5 向上取整）、受击坚韧（+6 层）、满载而归（取最大数量 + 每样 2 份）、翱翔（飞行速度 ×1.5）、淬魂之力（≤20% 斩杀 ×2.0）、拒绝篡改（30% 反弹）、旅者之力（审判范围 ×1.5 / 庇护可移动 30%） |
| **纯机制质变** | 虚空踏步（御风冲刺）、灵魂绑定（保经验）、全视之眼（威胁雷达）、环境免疫（免疫一切无实体来源伤害）、嗜血（过量吸血转护盾）、死亡抗拒（免费审判）、净魂（虚弱光环）、破敌之眼（禁无敌帧 3 秒）、恩赐永驻（正面无法驱散）、影杀（AOE 斩杀链）、真实血量（防秒杀底线 1 HP） |

---

## 共享基础设施

多个能力共用以下工具类，后文不再重复展开。

> **配置 TOML 定位**：所有配置键位于 `冒险能力配置 > 能力数值 > <能力名>` 子分组（每个能力一个子分组，如「灵巧」「伤害抗性」），觉醒相关键统一在 `冒险能力配置 > 能力数值 > 觉醒强化` 分组。各能力章节「配置：」行只列键名，未列分组前缀；`ModConfig.java` 中字段名即 TOML 键名（`AGILITY_BASE` → `agility_base`）。

### HealthUtil - 血量反射工具（`util/HealthUtil.java`）

对抗外部模组篡改血量的核心武器库。原理：原版血量存储在 `SynchedEntityData`（实体同步数据）中，`getHealth()`/`setHealth()` 只是访问器--外部模组常用 Mixin/ASM 拦截访问器，但很难拦截对底层数据结构的直接读写。

| 方法 | 用途 |
|------|------|
| `getHealthDirect()` | 反射拿 `DATA_HEALTH_ID`（SRG `f_20961_`）直接从 `SynchedEntityData` 读真实血量，绕过 `getHealth()` 覆写 |
| `setHealthDirect()` | 同理直接写入，绕过 `setHealth()` 覆写 |
| `setAllHealthLikeDirect()` | 写原版血量 + 通过 `scanCustomHealthKeys()` 扫描实体类继承链上所有 `EntityDataAccessor<Float>` 字段（带缓存），把自定义血量条目一并写入--对付把血存在自定义字段里的 Boss。**v1.2.5 两处增强**：① **参照血量选择**--`getHealth()` 与 `getHealthDirect()` 差 >1.0 时判定原版血条被架空（亚波伦等 Boss 重写 `getHealth()` 返回真实血量），改用 `getHealth()` 作参照，否则扫不到差距过大的自定义血量；② **值范围约束**--Float 值需落在参照血量 10% 容差内（或差 ≤1.0）才认定为自定义血量 key，排除史维特 `DATA_WATER_DAMAGE_SCALE` 等非血量 Float 误写（曾导致 `onSyncedDataUpdated -> Slime.move` 卡死） |
| `setAllHealthLikeRaw()` | 三步饱和写入：快照当前血量 -> `setAllHealthLikeDirect` -> 遍历 `SynchedEntityData.itemsById` 的所有 `DataItem`，对值≈快照值的条目**直接写 `DataItem.value` 字段**（支持 Float/Double/Integer），完全不经过 `data.set()`，绕过 dirty 标记、监听器和一切 Mixin |
| `clearNegativeFloatDeltas()` | 把 DataItem 中所有负值 Float 归零。反制某些 Boss 向玩家注入"负血量偏移量"再 ASM 篡改 `getHealth()` 叠加的手法。**仅供 TrueHealth 防御侧调用，攻击侧不用** |
| `restoreFoodData()` | 反射直写 `FoodData.foodLevel`（`f_38696_`）= 20、`saturationLevel`（`f_38697_`）= 20，绕过 `setFoodLevel()` 拦截 |
| `removeDirect()` / `setRemovedDirect()` / `setRemovedFieldDirect()` | 三种强度的实体移除：反射调 `remove()` -> 反射调 private `setRemoved()`（不参与虚分派）-> 直接写 `Entity.removalReason` 字段（`f_146795_`） |
| `clearRemovedFlag()` | 写 `null` 到 `removalReason`，把被外部标记移除的玩家"复活" |
| `eradicateFromWorld()` | 从 Minecraft 内部 6 个实体追踪结构直接抹除实体（详见[影杀](#影杀-shadow_kill)） |
| `HURT_DEPTH` | `ThreadLocal<Integer>` 计数器（字段非方法）：`hurt()` 调用链深度，由 `RejectHealthManipMixin` 维护（HEAD +1 / RETURN -1）、`TrueHealthMixin` 读取，用于区分"合法 hurt 内 setHealth"与"外部直调 setHealth"。**v1.3.1**：`resetHurtDepthPerTick()` 由 ServerTickEnd 无条件归零（hurt 不跨 tick，tick 末深度必为 0 是不变量），防御外部模组 cancel `hurt()` 导致的深度永久残留 |

### InvulClearUtil - 自定义无敌清除

`clearCustomInvulTimers(LivingEntity)` 遍历实体类继承链上的所有字段，按命名模式反射清除模组 Boss 的自定义无敌：

- `int` 字段名含 `invul`/`Invul` -> 置 0（如 Goety 的 `moddedInvul` / `obsidianInvul`）
- `boolean` 字段名含 `invul`/`Invul` -> 置 false
- `boolean` 字段名含 `vulnerable`/`Vulnerable` 且不含 `invul` -> 置 true（如 Mowzie's Mobs 钢铁守护者的 `vulnerable`）

> Java `String.contains()` 大小写敏感，故对每个模式显式覆盖大写开头变体（`Invul` / `Vulnerable`）。

### FriendlyFireProtection - 友伤保护

`isOwnerTarget(attacker, target)`：目标是 `TamableAnimal` 且主人是攻击者本人时返回 true。所有"超模"进攻能力（淬魂、影杀、破敌之眼、审判、嗜血）在生效前都先过这个检查，保证不会误伤自己的狼、猫或其他模组的驯服生物。

### FortuneContext - 跨 Mixin 上下文传递

两个 `ThreadLocal`（`CURRENT_BREAKER` / `AWAKENED`），解决"Mixin 注入点参数里没有玩家引用"的经典问题（详见[鸿运当头](#鸿运当头-fortune_favor)）。

### AbilityGate - 能力门禁与觉醒倍率

`isAbilityActive(player, id)` 封装"冒险者/觉醒 + 能力启用"三连门禁；`getActiveProgress(player, id)` 返回通过门禁的 `IAdventureProgress`（避免 `isAbilityActive` + 再查一次的双重查询）；`isActive(progress, id)` 一行门禁断言（已有 progress 对象时）；`value(progress, id)` 能力数值取值（`Optional<Float>`）；`awakenedRatio` / `awakenedPercent` 统一百分比能力的觉醒倍率计算（含 cap 钳制）。各 Handler 门禁与倍率统一走此类。

### AbilityIds - 能力 ID 常量

30 个能力 ID 的常量集中管理（`AGILITY` / `SHADOW_KILL` 等）。代码侧调用（`isAbilityEnabled` / `AbilityRegistry.get` / `getActiveProgress` / `toggleAbility`）统一引用此类，重命名/纠错可由 IDE 全局重命名，typo 从「运行时静默失效」降为「编译可查」。伤害类型 ID（`soul_strike` / `judgment` / `shadow_kill` 的 damage_type）与能力 ID 同名但语义不同，保持 `DamageUtil` 中的字面量。

### ProgressCache - 高频注入点门禁缓存

`get(entity)` 以实体为弱 key 缓存「本 tick 已解析的 progress 引用」（tick 变化即失效重算），供 `TrueHealthMixin.getHealth` / `RejectHealthManipMixin.getAttribute/setHealth` / `DeathDefyMixin.setHealth` 等 MC 极高频调用点使用——`getHealth` 每 tick 每玩家数十次，每次 `LazyOptional.resolve()` 代价昂贵。缓存的是引用而非门禁结果，`isAdventurer` / `isAbilityEnabled` 每次调用仍实时读内存字段，无延迟；弱 key 保证玩家实体 GC 后条目自动释放。

### PiercingGazeUtil - 破敌之眼穿透结算

`resolveAttacker(source)` 追溯真正攻击者：先取 `source.getEntity()`（MC 语义为责任/间接实体，通常即攻击者）-> 为空再取 `getDirectEntity()`（直接实体）-> 若为 Projectile 再取 owner；`postHurtEvent` 手动 post `LivingHurtEvent` 并取 `max(原值, 事件值)` 防 Boss 限伤；`invokeActuallyHurt` + `afterPierceFallback`（血量直写兜底 + 清自定义无敌字段）三连穿透结算，破敌之眼三层 Mixin 共用。

### DamageUtil - 内部伤害源

`isInternalSource(source)` 判断 `soul_strike` / `judgment` / `shadow_kill`（防淬魂/审判/影杀斩杀的递归重入）；`createSoulStrike` / `createJudgment` / `createShadowKill` 构造模组内部伤害源（绕过护甲/无敌/附魔/冷却）。

资源文件（三种内部伤害类型共用同一套 bypasses 标签）：

```
data/adventure_power/damage_type/{judgment,soul_strike,shadow_kill}.json
data/minecraft/tags/damage_type/bypasses_{armor,cooldown,enchantments,invulnerability}.json
```

`shadow_kill` 伤害类型另配 `death.attack.shadow_kill` 翻译键（影杀斩杀死亡消息，见 `assets/adventure_power/lang/{zh_cn,en_us}.json`）。

### ItemInventoryHelper - 物品栏遍历

`forEachAdventureSlot` / `forEachInventoryList` / `forEachInventorySlot` / `forEachCuriosSlot` 统一遍历玩家背包/盔甲/副手/Curios（`forEachInventorySlot` 仅背包/盔甲/副手的 ItemStack 粒度，不含 Curios），消除 `AdventureProgressCapability` 多处遍历样板。`hasAnyAdventureSlot` 短路判断存在性。

### PersistentDataKeys - NBT key 常量

集中管理所有自定义 persistentData key（`MME_AdventureProgress` / `AP_ShadowHP_Data` 等）+ `milestoneNbtKey(id)`，避免散落字符串和跨文件重复方法，存档兼容性修改只改一处。

### AdventureItemNbtUtil - 冒险饰品 NBT

`playerHasAdventureItem/End`、`recoverProgressFromItems`（从物品 NBT 兜底恢复 Capability）、`syncAllAdventureItemNbt`、`writeMilestonesToStack`、`migrateOldStage`（旧 AdventureStage 迁移）、`replaceBeginWithEnd`（全解锁时把冒险的开始替换为终点，搜索顺序 Curios -> 背包 -> 盔甲 -> 副手）。物品 NBT 是数据三层备份之一。从 AdventureProgressCapability 拆出。**兜底恢复附带激活**：恢复任一里程碑即同步 `activateAdventurer()`，恢复满 10 个时 `activateFullyUnlocked()` 并回写所有饰品 NBT（旧存档升级路径的关键行为）。

### SyncUtil - 数据同步

`syncCapabilityToPersistent`（Capability -> persistentData 显式同步，安全措施）+ `syncToClient`（向客户端推送含里程碑注册表元数据，供 tooltip 渲染 + 能力可用性判断）。grantMilestone / 各 handler / AdventureItemNbtUtil 共用。注册表元数据按 `MilestoneRegistry.getVersion()` 缓存，`/reload` 后版本号变化才重建——数据包更新后客户端 tooltip 未刷新时，先检查是否为版本号未变的缓存行为。

### ScoreboardUtil - 计分板

`updateScoreboard` 维护"冒险解锁"计分板目标（`adventure_power_unlock`），全解锁时置 1，供命令方块/数据包查询玩家全解锁状态。

### BuffExclusionManager - Buff 排除管理

`toggleBuffExclusion` / `getBuffExclusionSet` 管理恩赐永驻黑名单（玩家在统一面板 Buff永驻标签排除不想续期的正面效果），存 persistentData 跨死亡持久。

### NetworkHandler - 网络层

`network/NetworkHandler.java` 注册 8 个网络包（所有 C2S 处理均带方向校验 + 服务端门禁）：

| 包 | 方向 | 用途 |
|----|------|------|
| `AdventureSyncPacket` | S2C | 同步 Capability 数据（含里程碑注册表元数据），`SyncUtil.syncToClient` 发送 |
| `AdventureSyncRequestPacket` | C2S | 客户端登录/重连时请求同步（`sendToServer` 触发） |
| `DoubleJumpPacket` | C2S | 二段跳（空包，服务端校验门禁后施力） |
| `AbilityTogglePacket` | C2S | 能力面板开关（服务端持久化） |
| `BuffTogglePacket` | C2S | Buff永驻标签开关（恩赐永驻黑名单） |
| `BuffBlacklistSyncPacket` | 双向 | 黑名单同步（C2S 上报，S2C 广播更新） |
| `ActiveSkillPacket` | C2S | 释放主动技能（skillIndex，服务端重校验） |
| `SkillSwitchPacket` | C2S | 切换主动技能（v1.3.1 新增，服务端持久化并回同步） |

---

## 里程碑 1 · 初次夜冕

### 灵巧 `agility`

**效果**：受到任何伤害时有概率完全闪避（伤害归零，不触发后续减伤链）。闪避率 10% -> 73%（随里程碑成长），觉醒后 ×1.5，硬上限 `awaken_percent_cap`（默认 0.95）。

**实现**：
- 入口：`LivingAttackEvent` -> `CombatAbilityHandler.onLivingAttack()`
- 通过门禁后计算闪避率 `(AGILITY_BASE + AGILITY_PER_MILESTONE × (count - countAtUnlock)) / 100`，`player.getRandom().nextFloat()` 判定
- 命中闪避则 `event.setCanceled(true)` **在伤害进入减伤/护甲计算前整体取消**，并生成 20 枚 `WITCH` 粒子反馈
- 觉醒：`(value/100) × AWAKEN_MULTIPLIER`，硬钳制 ≤ `awaken_percent_cap`（默认 0.95），防止完全无敌

配置：`agility_base`(10) / `agility_per_milestone`(7) / `awaken_multiplier`(1.5) / `awaken_percent_cap`(0.95)

### 大地之力 `digging_power`

**效果**：挖掘速度 ×1.3 -> ×1.75，觉醒后约 ×2.625，且无视水中/空中挖掘惩罚。

**实现**：
- 入口：`PlayerEvent.BreakSpeed` -> `ExplorationAbilityHandler.onBreakSpeed()`
- `event.setNewSpeed(originalSpeed × multiplier)`，纯事件层实现，无 Mixin，兼容性最好
- 觉醒：反向补偿原版 `getDestroySpeed` 的挖掘惩罚 -- 眼在水中且无水下速掘附魔时 `/5`、未着地时 `/5`，觉醒时各 `×5` 补回，叠加 `×1.5` 倍率（飞行/游泳也无惩罚）

配置：`digging_power_base`(1.3) / `digging_power_per_milestone`(0.05) / `awaken_multiplier`(1.5)

---

## 里程碑 2 · 初尝败绩

### 虚空踏步 `void_step`

**效果**：空中可再次跳跃（二段跳），清空摔落距离。觉醒后获得「御风」：疾跑时二段跳朝视角方向水平冲刺，松开疾跑为纯垂直跳。

**实现**（客户端预测 + 服务端权威，两端共用 `VoidStepMovement` 施力公式）：
1. **客户端**（`JumpInputHandler`，钩子 `MovementInputUpdateEvent`，与原版跳跃同 tick 生效）：边沿检测 `input.jumping`（按一下跳一次，松开再按再触发）；本地维护 `clientJumpsUsed`（空中仅允许 1 次，落地清零）；条件满足时调 `VoidStepMovement.applyJump(player, power, dash)` 做即时预测（`dash = 觉醒 && 疾跑`，觉醒疾跑时附加御风冲刺），发送 `DoubleJumpPacket`
2. **服务端**（`DoubleJumpHandler.handleDoubleJump(player)`，网络包处理）：重新校验门禁（不骑乘/不在水中/不在飞行；**刻意不校验 onGround**——客户端已在 !onGround 时才发包，服务端再校会因网络延迟误杀）；`AIR_JUMPED` Set 记录本空中周期已跳（落地清零由 `PlayerTickDispatcher` 分发的 `DoubleJumpHandler.onTick` 检测 onGround 完成），每周期仅允许 1 次；**v1.3.1**：鞘翅滑翔 / 攀爬不触发二段跳（客户端与服务端条件对称）；调 `VoidStepMovement.applyJump(player, power, false)`（**只设 Y，不施加御风冲刺**，对齐云朵瓶：水平冲刺由客户端预测，位置客户端权威，避免两端 sprint 状态不同步导致水平跳变）
3. 跳跃力：`0.42 × 方块 JumpFactor + 跳跃提升加成`，整体 × `VOID_STEP_POWER`（默认 1.2）；Y **直接覆盖**（对齐原版 `jumpFromGround`，无 max 弹跳感）；`fallDistance = 0`；**成功不发包**（信任客户端预测，两端 Y 同公式无顿挫），拒绝时才发 `ClientboundSetEntityMotionPacket` 拉回防独飞
4. **`VoidStepMovement`**：两端共用 `calculateJumpPower` / `applyJump(power, dash)`；`dash=true` 时朝 `YRot` 施加 `AWAKEN_VOID_STEP_DASH` 水平冲刺。**v1.3.0 动量物理**：二段跳保留当前水平速度（`motion.x/z` 不变），跑得快自然跳得远；御风冲刺冲量随当前移速加成（`dashAmount × 当前移速 / 基础移速`），跑得快冲得远--与加速（`swift`）属性联动
5. 生命周期：登出 / 死亡重生（Clone）/ 落地清理 `AIR_JUMPED`；客户端在死亡重生/换维度（玩家引用变化）时重置 `clientJumpsUsed` / `wasJumpDown`，防旧玩家状态残留
- 觉醒「御风」：`dash = 觉醒 && isSprinting()`，疾跑时二段跳朝视角方向水平冲刺（`AWAKEN_VOID_STEP_DASH` 默认 0.6，随移速加成），松开疾跑纯垂直跳

配置：`void_step_power`(1.2) / `awaken_void_step_dash`(0.6)

### 休养生息 `rapid_recovery`

**效果**：脱离战斗 5 秒后，每 3 秒自动回血并回满饱食度。回量随里程碑成长，觉醒每周期额外 +5 HP。

**实现**（v1.1.9 起为直写方案）：
- 受伤记录：`RecoveryHandler.onLivingHurt()` 把每次受击时间写入 `lastHurtTimestamps[UUID]`（事件已被其他模组 cancel 时不记录，被取消的伤害不算进脱战计时）
- 恢复判定：`PlayerTickDispatcher` 分发的 `RecoveryHandler.onTick` 每 60 tick 检查一次，`当前时间 - 上次受伤 ≥ RAPID_RECOVERY_DELAY_TICKS`（100 tick）判定为脱战
- **直写回血**：`HealthUtil.setAllHealthLikeRaw(player, min(maxHP, 当前 + (amplifier+1) × rapid_recovery_heal_per_amplifier))`（每级 amplifier 折算回血量由配置控制，默认 1.0 HP）。**不使用再生药水效果**，避免被外部模组的 `MobEffectEvent` 拦截或 `removeAllEffects` 清除
- **直写饱食度**：`HealthUtil.restoreFoodData()` 反射写 `FoodData` 字段为 20/20
- 登录时 `putIfAbsent` 初始化受伤时间，防止刚上线被误判为"已脱战"立刻回血；登出清理两个 Map 防内存泄漏
- 觉醒：`amplifier += AWAKEN_RAPID_RECOVERY_BONUS`(5)

配置：`rapid_recovery_amplifier_base`(0) / `rapid_recovery_amplifier_step`(1) / `rapid_recovery_delay_ticks`(100) / `rapid_recovery_check_interval`(60) / `rapid_recovery_heal_per_amplifier`(1.0) / `awaken_rapid_recovery_bonus`(5)

---

## 里程碑 3 · 初次交易

### 灵魂绑定 `soul_bind`

**效果**：死亡时保留正面药水效果；觉醒时额外保留经验（不掉落）。

**实现**（利用 persistentData 跨越死亡）：
- **死亡时**（`LivingDeathEvent` @**LOW**，v1.2.3 调整：让死亡抗拒等可能取消死亡的逻辑先执行，避免"被救却已清零经验"）：**只序列化正面效果**（`BENEFICIAL`，负面随死亡清除）为 `CompoundTag` 存入 `player.getPersistentData()`（键 `AP_SoulBind_Buffs`）；经验以 **`CompoundTag` 三字段**（`level` / `progress` / `total`）存入 `AP_SoulBind_Exp`。选 persistentData 是因为 Capability 在死亡时会被 `invalidateCaps`
- **经验清零**（仅觉醒）：备份经验后清零 `experienceLevel` / `experienceProgress` / `totalExperience`，清零后死亡就不会喷出经验球；非觉醒不备份也不清零，经验照常掉落捡回（原版行为）
- **重生时**（`PlayerEvent.Clone`，`isWasDeath()` 分支）：从 `event.getOriginal()` 的 persistentData 读回，逐个 `MobEffectInstance.load()` + `addEffect()`；经验**直接赋值三字段 + 发 `ClientboundSetExperiencePacket` 同步**（避免 `giveExperiencePoints` 的 `increaseScore` 副作用与 level 重算偏差），随后删除两个键防重复恢复
- 非死亡 Clone（维度切换）**不再转移** soul_bind 数据--`transferSoulBindData()` 已移除：`SOUL_BIND_*_KEY` 仅死亡时写入并由死亡分支消费；维度切换分支做**防御性清理**——死亡写入但未消费的残留 key（如被其他模组取消死亡事件后走维度切换）会被显式删除。经验由原版 `restoreFrom` 复制，buff 由原版保留

配置：无（解锁即完整）

### 不动如山 `knockback_resist`

**效果**：击退抗性 30% -> 79%，觉醒 ×1.5（硬上限 100%）。

**实现**：
- 入口：`PlayerTickDispatcher` 分发的 `KnockbackResistHandler.onTick()` 每 tick 对账
- **直接写属性基值**：`Attributes.KNOCKBACK_RESISTANCE` 的 `setBaseValue(percent/100)`，让原版击退计算自行消化，与其他模组的击退修改天然兼容（不用 AttributeModifier 是因为只动基值、无 UUID 冲突问题）
- 对账逻辑（**v1.2.8 简化**）：统一计算期望值（启用=percent/100，禁用=0），仅在 |当前值-期望值| > 0.001 时写入，覆盖启用/禁用/里程碑变化三种情况 -> 写入；能力关而有值 -> 归零；`Clone` 重写。**v1.3.1**：首次写入前记录原值（`ORIGINAL_KNOCKBACK_RESIST`），登出恢复原值而非硬编码 0，不覆盖其他模组对击退抗性 baseValue 的持久化修改

配置：`knockback_resist_base`(30) / `knockback_resist_per_milestone`(7) / `knockback_resist_hard_cap`(100.0) / `awaken_multiplier`(1.5)

### 加速 `swift`

**效果**：常驻提升移速（类似迅捷药水的额外附加速度，无图标），水下额外加速（海豚祝福），疾跑时大幅降低饱食消耗。觉醒后疾跑碰撞推开敌对生物。

**实现**：
- **移速加成**：`ExplorationAbilityHandler.syncSpeedAttribute()` 每 tick 对账，给 `Attributes.MOVEMENT_SPEED` 加 `AttributeModifier`（operation=`MULTIPLY_TOTAL`，与迅捷药水同 operation，作为额外附加速度，**不改 baseValue**）。用 Forge 的 `addTransientModifier`（1.20.1 vanilla `addModifier` 为 private）。**仅在 bonus 变化时 remove+add**（检查 `getModifier`），避免每 tick 开销。bonus 随里程碑线性成长，觉醒 ×`AWAKEN_MULTIPLIER`
- **水下加速**：`SwiftHandler.onTick()` 检测 `player.isInWater()` 时循环刷新 `DOLPHINS_GRACE`（海豚祝福，无图标无粒子），常驻不限疾跑
- **饱食消耗补偿**：疾跑时 `foodData.addExhaustion(-0.1 × SWIFT_EXHAUSTION_REDUCTION)`，抵消原版疾跑消耗的比例（原版约 0.1/tick，负值不消耗食物仅累积回正）
- **觉醒推开**：疾跑时限频每 5 tick 对半径内 `Monster` 朝外施加 `setDeltaMovement`（带 0.3 垂直分量；`setDeltaMovement` 后置 `hurtMarked = true` 同步位置变更；停止疾跑时立即清除限频记录——重新疾跑第一 tick 即可推开，而非严格 5 tick）
- `Clone` 恢复、登出移除 modifier 防残留
- **与虚空踏步联动**：`VoidStepMovement` 御风冲刺读 `MOVEMENT_SPEED` 当前值/基础值，加速越高御风冲得越远（动量物理）

配置：`swift_speed_base`(0.05) / `swift_speed_per_milestone`(0.02) / `swift_exhaustion_reduction`(0.8) / `swift_water_duration`(220) / `awaken_multiplier`(1.5) / `awaken_swift_push_radius`(3.0) / `awaken_swift_push_strength`(0.6)

---

## 里程碑 4 · 初探地底

### 全视之眼 `all_seeing`

**效果**：永久夜视（无图标无粒子）+ 消除下界/末地/水下雾气。觉醒后右下角显示威胁雷达（附近敌对生物 3D 方位 + 名称 + 距离）。

**实现**：
- **夜视**：`AllSeeingHandler.onTick()` 循环刷新 `MobEffects.NIGHT_VISION`（余量低于一半才 `addEffect`，避免每 tick 同步），`ambient=false, visible=false, showIcon=false` 无粒子无图标
- **去雾**：客户端 `AllSeeingClientHandler` 订阅 `ViewportEvent.RenderFog`，读 `ClientHudDataCache.allSeeingEnabled`，启用时把雾平面推远到 4096（消除下界/末地/水下的近距离雾）——仅 `farPlaneDistance < 4096` 时推远，且近平面强制设 0.05（部分渲染器对 0 近平面敏感）
- **觉醒雷达**：客户端 `ClientHudDataCache.scanRadar()` 限频每 `awaken_all_seeing_radar_scan_interval`（10 tick）扫描半径 `awaken_all_seeing_radius`（24）内 `Monster`，先按 **3D 平方距离**排序（有意与显示距离一致，保证最近目标就是列表第一个）并截断到 `awaken_all_seeing_radar_max`（6）个再算方向（避免对全部实体做翻译查找），算 3D 方位文字（水平 8 × 垂直 3 自由组合：正前/左前/前上/左前下/正上 等；**v1.3.1 起方位词走 `hud.adventure_power.dir.*` 翻译键本地化**——四正方位（front/back/left/right）走带 `_cardinal` 后缀的键、对角走裸键；水平距离 ≤ 0.5 时直接判正上/正下；翻译结果静态缓存，切换语言需重开游戏生效），`AllSeeingRadarOverlay` 在右下角右对齐列表渲染
- 方位计算：水平角 `atan2(-dx, dz)` 与 MC yaw 同系，相对玩家前方为「正前」；垂直角 `atan2(dy, horizDist)`，>60° 归正上、<-60° 归正下、±30° 外按水平词+上/下

配置：`all_seeing_night_vision_duration`(220) / `awaken_all_seeing_radius`(24) / `awaken_all_seeing_radar_max`(6) / `awaken_all_seeing_radar_scan_interval`(10)（v1.3.1 统一 SEEING 拼写，旧 `awaken_all_seing_*` 键失效重置为默认值）

### 无形之手 `extended_reach`

**效果**：方块交互距离 + 攻击距离 +1.0 -> +2.2 格（两者同步），觉醒 ×1.5。

**实现**：
- 入口：`ExplorationAbilityHandler.onTick()`(END，由 `PlayerTickDispatcher.onPlayerTick` 分发) -> `syncReachAttribute()` 每 tick 对账
- **同时设两个 Forge 专有属性**：`ForgeMod.BLOCK_REACH`（方块触及）+ `ForgeMod.ENTITY_REACH`（实体触及=攻击距离），共用 `applyReachAttr()` 辅助方法，`expected = 默认值 + bonus`，误差 > 0.001 才写入（防抖）
- `Clone` 恢复；**v1.3.1**：首次写入前分别记录原值（`ORIGINAL_REACH` / `ORIGINAL_ENTITY_REACH`），**关闭能力/登出时 BLOCK_REACH 与 ENTITY_REACH 都恢复原值而非默认值**（不覆盖其他模组对触及距离 baseValue 的持久化修改）。从未记录过原值（从未写入过，或崩溃/强杀重启内存 Map 清空）时做**残留判定**：当前值 ≈ 本模组开启时会写的值（默认 + `activeBonus`）→ 判定为本模组残留 → 回归默认值；否则视为其他模组的修改，记录当前值不碰

配置：`extended_reach_base`(1.0) / `extended_reach_per_milestone`(0.2) / `awaken_multiplier`(1.5)

### 磁吸 `magnet`

**效果**：自动吸取半径内的掉落物至玩家背包。吸取半径随里程碑成长，觉醒后范围扩大并额外吸取经验球。

**实现**：
- 入口：`MagnetHandler.onTick()`（由 `PlayerTickDispatcher` 分发），限频每 `MAGNET_SCAN_INTERVAL`（5 tick）扫描一次降低 `getEntities` 开销
- 半径 = `MAGNET_RADIUS_BASE + MAGNET_RADIUS_PER_MILESTONE × (count - countAtUnlock)`，觉醒 ×`AWAKEN_MAGNET_RADIUS_MULT`
- `serverLevel.getEntitiesOfClass(ItemEntity.class, AABB)` 取范围内掉落物，对每个调 `attract()`：`setPos` lerp 朝玩家**躯干中心**（`player.getY() + 0.5`，避免贴地物品角度过陡）移动 `magnet_pull_factor`（默认 0.3=30%）距离（穿墙，磁吸特性）+ 清零惯性防重力干扰，`setPos` 后置 `hurtMarked = true`（不置则服务端位置变更不同步到客户端）；进入 `MAGNET_PICKUP_RADIUS` 时调 `entity.playerTouch(player)` 触发原版拾取（内部校验 `pickupDelay`，未到则下个 scan 再试）
- 觉醒：`AWAKEN_MAGNET_INCLUDE_XP` 为 true 时额外扫描 `ExperienceOrb` 吸取
- 登出清理扫描时间缓存防内存泄漏

配置：`magnet_radius_base`(5.0) / `magnet_radius_per_milestone`(0.5) / `magnet_pickup_radius`(1.5) / `magnet_scan_interval`(5) / `magnet_pull_factor`(0.3) / `awaken_magnet_radius_mult`(1.5) / `awaken_magnet_include_xp`(true)

---

## 里程碑 5 · 初次附魔

### 不朽装备 `undying_gear`

**效果**：所有装备耐久永不消耗。觉醒后每件护甲 +2 护甲值、主手武器 +25% 伤害（独立配置）。

**实现**（`LegacyHurtAndBreakMixin`，双注入点缺一不可）：
- `ItemStack.hurtAndBreak()`（SRG `m_41622_`）@HEAD：原版耐久扣除入口，`ci.cancel()` + `setDamageValue(0)` 回满
- `ItemStack.hurt()`（SRG `m_220157_`）@HEAD：**Forge 1.20.1 新增的独立耐久扣除方法，不经过 hurtAndBreak**，部分模组直接调它。`cir.setReturnValue(false)`（"未损坏"）+ `setDamageValue(0)`
- 门禁走静态方法 `AdventurePower.hasUndyingGear(player)`（Mixin 类中禁止非 private static，门禁逻辑放主类）
- 觉醒加成不在此 Mixin，由属性 Handler 单独处理

配置：`awaken_undying_armor_bonus`(2.0) / `awaken_undying_weapon_bonus`(0.25)

### 鸿运当头 `fortune_favor`

**效果**：时运与抢夺等级 +1 -> +3（每 2 里程碑一档），在工具已有附魔上叠加。觉醒额外 +2。

**实现**（时运与抢夺路径完全不同）：
- **抢夺**：`LootingLevelEvent` -> `FortuneFavorHandler.onLootingLevel()`，`event.setLootingLevel(原值 + bonus)`--Forge 有现成事件，纯事件层
- **时运**：原版没有对应事件，需要 Mixin + 上下文传递：
  1. `BlockEvent.BreakEvent` 时：`FortuneContext.clear()`（先清防残留）-> `setBreaker(player)`，觉醒时 `setAwakened(true)`
  2. `FortuneFavorMixin` 注入 `EnchantmentHelper.getItemEnchantmentLevel()`（SRG `m_44843_`）@RETURN：查询的是 `BLOCK_FORTUNE` 时读 `FortuneContext.getBreaker()`，算出 bonus 后 `cir.setReturnValue(原值 + bonus)`
- **为什么要 ThreadLocal 上下文**：`getItemEnchantmentLevel(Enchantment, ItemStack)` 的参数里没有玩家--`ItemStack` 不持有主人引用，无法从注入点推导"谁在挖"。`BreakEvent` 与附魔查询在同一调用链上，用 `ThreadLocal` 桥接

配置：`fortune_favor_bonus_base`(1) / `fortune_favor_bonus_step`(1) / `awaken_fortune_favor_bonus`(2)

### 经验加成 `xp_boost`

**效果**：拾取经验球时经验 ×1.25 -> ×1.5（随里程碑成长，`1.25 + 0.05 × (10-5)`），觉醒后再 ×1.5。

**实现**：
- 入口：`PlayerXpEvent.PickupXp` -> `XpBoostHandler.onPickupXp()`，事件型（独立 `@EventBusSubscriber`，不走 tick 分发器）
- **补差额方案**：**不 cancel、不反射**--在原版拾取流程（含经验修补附魔、拾取音效与粒子）之上，额外 `player.giveExperiencePoints(baseXp × (mult - 1))`。原版给 base，此处补差额，合计 base × mult。完整保留经验修补/音效，无需 cancel/反射/SRG 名。**事件已被其他模组 cancel 时不补发**（`event.isCanceled()` 前置放行，尊重取消语义）
- 倍率 `mult = XP_BOOST_BASE + XP_BOOST_PER_MILESTONE × (count - countAtUnlock)`，觉醒 ×`AWAKEN_XP_BOOST_MULT`

配置：`xp_boost_base`(1.25) / `xp_boost_per_milestone`(0.05) / `awaken_xp_boost_mult`(1.5)

---

## 里程碑 6 · 炽热之门

### 环境免疫 `env_immunity`

**效果**：免疫火焰、冻结、雷击、摔落、溺水、仙人掌、凋零等环境伤害。觉醒后免疫**一切无实体来源的伤害**。

**实现**：
- 入口：`LivingAttackEvent` -> `PlayerStateHandler.onEnvDamage()`，在伤害进入结算前取消
- 判定顺序：
  1. 觉醒分支：`source.getEntity() == null && source.getDirectEntity() == null`（完全无实体来源）-> 直接取消，不限类型
  2. 非觉醒：有实体来源的一律放行（火焰弹的火伤算战斗伤害，不免疫）
  3. 标签匹配：`DamageTypeTags.IS_FIRE / IS_FREEZING / IS_LIGHTNING / IS_FALL / IS_DROWNING`
  4. 字符串兜底：标签没覆盖的类型（仙人掌、甜浆果、钟乳石、窒息、岩浆块、饥饿、凋零、干涸）按 `source.getMsgId()` 比对
- 辅助：`PlayerTickHandler.onTick` 每 tick 在 `getRemainingFireTicks() > 0` 时 `player.clearFire()`，对付直接 `setRemainingFireTicks` 的模组

配置：无

### 嗜血 `lifesteal`

**效果**：攻击时按伤害 5% -> 13% 吸血（单次上限最大生命 20%）。觉醒后过量吸血转为吸收护盾（上限最大生命 20%）。PVP 无效。

**实现**（v1.2.0 起为直写方案）：
- 入口：`LivingHurtEvent` @**LOW**（v1.3.1：基于减伤后的最终金额吸血，消除与伤害抗性的同优先级顺序不确定性）-> `RecoveryHandler.onLivingHurtLifesteal()`
- 门禁链：攻击者是玩家、目标非玩家、非内部伤害源（`soul_strike`/`judgment`/`shadow_kill`，防止淬魂/影杀斩杀追加伤害二次吸血）、友伤保护
- **直写回血**：`HealthUtil.getHealthDirect()` 读真实血量 -> `setAllHealthLikeRaw(attacker, min(maxHP, 当前+吸血量))`。不走 `heal()`，因为部分模组（如泽林变体）会在 `LivingHealEvent` HEAD 直接 cancel 玩家的所有治疗
- 觉醒：吸血量超出补满所需的部分 `excess` 转护盾--`setAbsorptionAmount(min(现有 + excess, maxHP × 20%))`。**v1.2.8 修复**：满血时（`healthBeforeHeal >= maxHealth`）全部吸血量转护盾（之前 `toFull > 0` 条件导致满血时 excess=0 不转护盾，觉醒嗜血失效）

配置：`lifesteal_base`(5) / `lifesteal_per_milestone`(2) / `lifesteal_cap_ratio`(0.2) / `awaken_lifesteal_shield_cap`(0.2)

### 伤害抗性 `damage_resist`

**效果**：受到的所有伤害减免 10% -> 30%（随里程碑成长，**仅对玩家目标生效**），觉醒 ×1.5，硬上限 `awaken_percent_cap`（默认 0.95，即 45%）。

**实现**：
- 入口：`LivingHurtEvent` -> `CombatAbilityHandler.handleDamageResist()`
- `event.setAmount(amount × (1 - ratio))`，最轻量的事件层减伤；受伤方门禁保留 `isCanceled()` 检查（与攻击方能力的 `receiveCanceled` 分工相反）

配置：`damage_resist_base`(10) / `damage_resist_per_milestone`(5) / `awaken_multiplier`(1.5) / `awaken_percent_cap`(0.95)

> v1.3.0 起从里程碑 4 调整至里程碑 6（炽热之门），配合环境免疫 + 嗜血构成下界生存三件套。

---

## 里程碑 7 · 凋零之陨

### 禁疗之触 `healing_block`

**效果**：攻击为目标施加 3 -> 6 秒"禁疗之触"标记：期间禁止一切治疗、禁止不死图腾、血量只能降不能升，标记期间死亡无法被取消。觉醒禁疗+易伤20%。淬魂之力对标记中的目标伤害 ×1.5。

**实现**：入口 `LivingHurtEvent` -> `CombatAbilityHandler.handleHealingBlock()` -> `HealingBlockEffect.apply(target, ticks)`。`HealingBlockEffect` 名义上是 `MobEffect`（暗红色图标仅作视觉指示），**真实判定依据是 NBT 标记** `MME_UndyingSlashEndTime`（`persistentData`，防止效果被 `removeAllEffects` 清掉后禁疗失效）。三层拦截：

1. **事件层**（`HealingBlockEffect.EventHandler`）：
   - `LivingHealEvent` -> cancel（禁一切治疗）
   - `LivingUseTotemEvent` -> cancel（禁图腾）
   - `LivingTickEvent`：**v1.2.8 改为仅过期清理**（钳制由 HealingBlockMixin tick TAIL 覆盖，避免重复）；全链路改用 `getHealthDirect`（防 ASM 改写 getHealth）
   - `LivingDeathEvent` @HIGHEST：写 `MME_UndyingSlash_ForceKill` 标记；@LOWEST：若死亡事件被其他模组 cancel 且存在 ForceKill 标记，强制 `setAllHealthLikeRaw(entity, 0)` + `setCanceled(false)`--**死亡锁定**（两处监听均 `receiveCanceled=true`，确保被其他模组 cancel 后仍能触发 uncancel）；**@LOWEST 分支无论是否触发 ForceKill 都清除跟踪数据与 NBT 标记**，防止复活后残留禁疗
   - `ServerTickEvent`(END)：对所有被追踪实体做最终钳制，覆盖完全不调 `setHealth()` 的 Boss；2 tick 宽限期容忍跨维度传送
2. **Mixin 跟踪层**（`HealingBlockMixin`，`setHealth` @RETURN）：记录每次合法降血后的新低水位
3. **Mixin 钳制层**（`tick` @TAIL）：AI/回血/事件全部跑完后最后检查一次，高于低水位即直写钳回

**Boss 二阶段豁免**：凋零/末影龙死亡触发二阶段转换时清空标记放行，防止卡死 Boss 战斗流程。

配置：`healing_block_base`(3) / `healing_block_per_milestone`(1) / `awaken_healing_block_vuln`(1.2) / `soul_quench_healing_block_multiplier`(1.5) / `healing_block_allow_boss_phase_two`(true，是否允许 Boss 二阶段)

### 坚韧之躯 `vitality`

**效果**：最大生命 +4 -> +10（半颗心计），觉醒 ×1.5 向上取整。

**实现**：
- 入口：`ExplorationAbilityHandler.onTick()`(END，由 `PlayerTickDispatcher.onPlayerTick` 分发) -> `syncVitalityAttribute()`，直写 `Attributes.MAX_HEALTH` 基值为 `20 + bonus`
- 关闭/上限下降时先把当前血量裁剪到新上限内再改属性，防止血量悬空
- 觉醒用 `Math.ceil(bonus × 1.5)` 而非浮点乘法，保证至少多 1 点
- `Clone` 恢复；**v1.3.1**：首次写入前记录原值（`ORIGINAL_MAX_HEALTH`），登出/关闭能力恢复原值而非硬编码 20，不覆盖其他模组（耐力/体质类）以 baseValue 持久化的 maxHealth 修改

配置：`vitality_base`(4.0) / `vitality_per_milestone`(2.0) / `awaken_multiplier`(1.5)

### 死亡抗拒 `death_defy`

**效果**：致死伤害不死，反而回满血 + 清负面 + 60 秒无敌，冷却 5 分钟。觉醒后触发时免费释放一次旅者审判。HUD 显示金色边框和倒计时。

**实现**（三层拦截保证 60 秒无敌"真"无敌）：
1. **事件层**（`LivingDeathEvent` @HIGHEST -> `DeathDefyHandler.onPlayerDeath()`）：冷却检查通过则 `setCanceled(true)`，清 HARMFUL 效果 + `clearFire()`，血量恢复 `max(20, getMaxHealth())`--**双写**：先 `HealthUtil.setHealthDirect`（绕过覆写）再 `player.setHealth`（保证 Forge 监听器和客户端同步）。写入 `deathDefyInvulEnd/CooldownEnd`（Capability NBT 持久化，跨下线）。触发时播放图腾音效 + 50 枚图腾粒子
2. **Mixin 层 1**（`DeathDefyMixin`，`setHealth` @HEAD，priority=1500）：无敌期内拒绝一切降血写入（回血放行），同时过滤 NaN/Infinity
3. **Mixin 层 2**（`tick` @TAIL）：无敌期内每 tick 检查 `getHealthDirect() < 20` 即双写拉回--反制通过 VarHandle 直写 `DataItem.value` 绕过 `setHealth` 的外部手段。钳制目标固定 20 而非 maxHealth，防外部临时改 maxHealth 污染判定
- HUD（`DeathDefyHudOverlay`，`RenderGuiOverlayEvent.Post`）：无敌期内四边金色半透明边框 + 顶部剩余秒数
- 觉醒联动：触发时若启用 `active_skill`，直调 `ActiveSkillHandler.executeJudgment()`，**不消耗冷却不占 GCD**

配置：`death_defy_invul_duration`(1200) / `death_defy_cooldown_duration`(6000)

> v1.3.0 起从里程碑 9 调整至里程碑 7（凋零之陨），主题契合"对抗凋零致死"。长冷却（5 分钟）平衡前期获取。

---

## 里程碑 8 · 幽匿之惧

### 受击坚韧 `resilience`

**效果**：每次受击叠 1 层，每层减伤 5%，5 秒未受击清零。上限 5/8/12 层（里程碑 8/9/10），觉醒 +6 层（最高 18 层 = 90% 减伤）。

**实现**：
- 入口：`LivingHurtEvent` @**LOW** -> `PlayerStateHandler.onLivingHurt()`--低优先级保证拿到其他减伤都算完后的最终伤害
- 先按当前层数减伤 `event.setAmount(amount × (1 - stacks × 0.05))`，再叠层（不超过 `ResilienceAbility.value(count, awakened)` 上限）、刷新 `lastHurtTime`
- 层数与时间戳存 Capability（NBT 键 `resilienceStacks` / `lastHurtTime`），跨重生持久
- 过期清零：`PlayerTickHandler.onTick` 检查 `当前 - lastHurtTime ≥ RESILIENCE_RESET_TICKS`(100)

配置：`resilience_stacks_8`(5) / `resilience_stacks_9`(8) / `resilience_stacks_10`(12) / `resilience_damage_reduction_per_stack`(0.05) / `resilience_reset_ticks`(100) / `awaken_resilience_bonus_stacks`(6)

### 净魂 `purified_soul`

**效果**：免疫所有负面药水效果（根本不会被施加，无图标闪烁）。觉醒后周围 16 格敌对生物持续获得虚弱 II。

**实现**（Mixin 源头拦截 + tick 兜底双轨）：
- **Mixin 层**（`PurifiedSoulMixin`，priority=2000）：注入 `LivingEntity.canBeAffected()`（SRG `m_7301_`）@HEAD，效果类别为 `HARMFUL` 且玩家启用净魂时 `cir.setReturnValue(false)`--原版施加逻辑在入口就被判"免疫"，**效果从未被添加过**。门禁与其他能力 Mixin 统一：`ProgressCache` 按 tick 缓存 progress 引用 + `isAdventurer() || isFullyUnlocked()` 身份检查 + `isAbilityEnabled`
- 为什么不用 `MobEffectEvent.Applicable`：该事件在 Forge 1.20.1 不可取消，调 `setCanceled()` 会抛 `UnsupportedOperationException`
- **tick 兜底**：`PlayerStateHandler.onTick` 每 tick 扫一遍 `getActiveEffects()`，收集后统一移除漏进来的 HARMFUL 效果（先收集再 remove 防 ConcurrentModificationException；覆盖 NBT 直接注入、药水云等 Mixin 拦不到的路径）
- 觉醒虚弱光环：每 `awaken_purified_soul_aura_interval`（40）tick 对半径 `awaken_purified_soul_radius`（16）内的 `Monster` 施加 `awaken_purified_soul_weakness_amplifier`（默认 1=虚弱II）级虚弱，持续 `awaken_purified_soul_weakness_duration`（默认 100 tick）；刷新余量 = min(时长的 60%, 施加间隔)，配置短时长也不断档。施加错峰 `(gameTime + playerId) % interval` 分散服务器压力，重施条件为 amplifier 或时长不足（不每次覆盖）

配置：`awaken_purified_soul_radius`(16) / `awaken_purified_soul_aura_interval`(40) / `awaken_purified_soul_weakness_amplifier`(1) / `awaken_purified_soul_weakness_duration`(100)

### 满载而归 `loot_all`

**效果**：击杀生物后，在原版掉落基础上额外按掉落表"每样一份"追加掉落，无视 entry/pool/function 三级 LootItemCondition（特定方法杀 Boss、火杀熟肉等条件性掉落不用满足也掉）。觉醒后每样取掉落表最大数量。

**实现**（LivingDropsEvent + 4 Mixin + 双 ThreadLocal 标志位）：
- 入口：`LootAllHandler.onLivingDrops()` @FORGE -> `killer instanceof ServerPlayer` + `isAbilityEnabled("loot_all")` 门禁 + **友伤保护**（`FriendlyFireProtection.isOwnerTarget`，杀自家驯服生物不掉额外掉落），**不清空原版 drops**，额外调 `LootTable.getRandomItems(params)` 追加 `ItemEntity`，`max_items` 上限防极端配置卡服。构造 params 时显式传入 `THIS_ENTITY`/`ORIGIN`/`DAMAGE_SOURCE`/`KILLER_ENTITY`/`DIRECT_KILLER_ENTITY`/`LAST_DAMAGE_PLAYER` 六参数（`LAST_DAMAGE_PLAYER` 影响 `killed_by_player` 类条件的判定）
- **Mixin 1**（`LootPoolEntryContainerMixin`）：`@Inject` `canRun`（SRG `m_79639_`）@HEAD，BYPASS=true 时返回 true -> 无视 **entry 级**条件
- **Mixin 2**（`LootPoolMixin`）：`@Inject` `addRandomItems`（SRG `m_79053_`）@HEAD cancellable，BYPASS=true 时遍历 `entries` 各调 `expand` -> `createItemStack`（打破 weight 单选，每样一份）+ cancel 原 rolls 逻辑 -> 无视 **pool 级**条件
- **Mixin 3**（`LootContextBuilderMixin`）：`@ModifyArg` `LootContext.Builder.create`（SRG `m_287259_`）拦截 `new LootContext` 构造的 random 参数（index=1），AWAKEN=true 时注入 `ConstantMaxRandomSource` -> `SetItemCount` 取最大数量
- **Mixin 4**（`LootItemConditionalFunctionMixin`）：`@Redirect` `apply` 内 `compositePredicates.test`，BYPASS=true 时返回 true -> 无视 **function 级**条件（如 `SmeltItemFunction` 的 `on_fire`，非火杀也熔炼成熟肉）。**`require = 0, remap = false`**：MC 版本升级后若 `apply()` 内联该调用，注入会静默失效且启动不报错——满载归还唯一可能无警告退化的层
- 标志位 save-restore：`BYPASS`/`AWAKEN` 用 `ThreadLocal`，`finally` 恢复前值（防嵌套 LivingDropsEvent 重入破坏外层标志位）
- `entries` 字段经 `LootPoolAccessor` `@Accessor` 暴露（refmap `f_79023_`）；抢夺通过 `KILLER_ENTITY` 自动随玩家（1.20.1 `getLootingModifier` -> `ForgeHooks.getLootingLevel`）

**边界**（非 loot table 掉落无法覆盖）：
- Forge GlobalLootModifier (GLM) 类掉落（如星月纯净之星）- 独立于 loot table，拿不到
- `dropCustomDeathLoot` 代码 spawn（如诡厄巫法隐匿蓝图）- 不走 loot table，拿不到
- `AlternativesEntry`（or）只给首个 alternative - 符合"任选一"语义，非 bug

配置：`loot_all_copies`(1) / `loot_all_max_items`(100) / `loot_all_awakened_max_count`(true) / `loot_all_awakened_copies`(2)

---

## 里程碑 9 · 终末之翼

### 翱翔 `soar`

**效果**：获得创造模式飞行。觉醒飞行速度 ×1.5。

**实现**（核心难点：与其他模组的飞行来源共存）：
- 入口：`PlayerStateHandler.onTick()`(END，由 `PlayerTickDispatcher.onPlayerTick` 分发) 每 tick 对账
- 授予：`abilities.mayfly = true` + `onUpdateAbilities()`，同时在 Capability 记 **`soarGrantedFlight = true`**--"这个 mayfly 是翱翔给的"。注意 `grantMilestone()` 解锁即时授予与开局安全网两条路径**不置** `soarGrantedFlight`（依赖下一 tick onTick 补标记）
- **精准回收**（v1.1.6 修复）：tick 对账路径关闭能力时必须同时满足 ①当前 mayfly=true ②`soarGrantedFlight == true` ③非创造非旁观，才回收。**装备鞘翅环、其他模组给的飞行不会被误没收**。能力面板（`AbilityTogglePacket`）手动关闭走 `applySoarState(false)`，同样查 `soarGrantedFlight`（与 tick 对账一致，回收时同步置 false）——两条关闭路径行为统一；回收时同时 `flying = false` + `flyingSpeed` 重置 0.05
- 飞行速度写 `flyingSpeed` 字段，只在与目标值偏差 > 0.0001 时写入--防止每 tick 重复乘法导致速度指数爆炸
- 时序处理：`Clone` 跨维度保留飞行中状态；`PlayerChangeGameModeEvent` 立即重新授予（**v1.2.8：玩家在空中时自动设 `flying=true` 防坠落**，地面保留双击空格触发）；`grantMilestone()` 解锁时立即同步（不等下一 tick，消除竞态）

配置：`awaken_soar_speed`(1.5)

### 淬魂之力 `soul_quench`

**效果**：攻击附加真实伤害：`固定值 + 目标最大生命% + 当前生命%`（9/10 里程碑两档：2+1%+1% / 4+2%+2%）。对禁疗标记中的目标 ×1.5。觉醒后目标血量 ≤`awaken_soul_quench_execute_threshold`（默认 20%）时伤害 ×`awaken_soul_quench_execute_multiplier`（默认 2.0，斩杀线）。

**实现**：
- 入口：`LivingHurtEvent` -> `CombatAbilityHandler.handleSoulQuench()`
- 追加伤害用**自定义伤害类型 `adventure_power:soul_strike`**（`data/adventure_power/damage_type/soul_strike.json`，datapack 标签配置为绕过护甲/无敌/附魔/攻击冷却）走一次独立 `target.hurt()`
- 防递归：`isInternalSource()` 检查伤害源 msgId 是 `soul_strike`/`judgment`/`shadow_kill`（v1.3.1 起含影杀）则跳过（否则自己的追加伤害会再触发自己）
- 伤害后立即 `invulnerableTime = 0` + 反射清 `hurtTime`（部分 Boss 拿 `hurtTime>0` 当额外无敌判据）
- **直写补刀**：比较 `实际造成 < 期望 - ε`，说明 hurt 被外部拦截 -> `setAllHealthLikeRaw` 直写修正后血量；归零则先 `setLastHurtByMob/Player(attacker)` + `attacker.awardKillScore(...)`（任务模组/计分板正确归击杀者）再反射置 `deathScore = -1`（防重复计分）后强制 `target.die(source)`

配置：`soul_quench_flat_damage_9/10`(2/4) / `soul_quench_hp_ratio_9/10`(0.01/0.02) / `soul_quench_healing_block_multiplier`(1.5) / `awaken_soul_quench_execute_threshold`(0.2) / `awaken_soul_quench_execute_multiplier`(2.0)

### 破敌之眼 `piercing_gaze`

**效果**：攻击无视一切无敌--原版无敌帧、模组 Boss 的自定义无敌、限伤、"必须先破条件才能打"的机制统统穿透。觉醒后破无敌一击，目标 3 秒内无法获得无敌帧。

**实现**：模组内最复杂的拦截体系，5 层纵深防御分布在 4 个 Mixin + 1 个事件 Handler。核心思路：**Boss 的"无敌"实现五花八门，每层针对一类实现**。

| 层 | 位置 | 针对的无敌实现 |
|----|------|---------------|
| **Layer 0** | `PiercingGazePlayerAttackMixin`：`@Redirect` 替换 `Player.attack()`（`m_5706_`）内部的 `target.hurt()` 调用 | 完全重写 `hurt()` 且不调 `super.hurt()` 的 Boss（如暮色森林巫妖）--此时 Layer 2/2.5 的注入点根本不会执行。**v1.2.8 修复**：`@At` target owner 改为 `Entity`（匹配字节码 INVOKEVIRTUAL owner）+ handler 参数改为 `Entity`（匹配 owner，`instanceof LivingEntity` 转型处理）+ **血量比对**（`getHealthDirect` 直读 DataItem，实际扣血才放行，覆盖三类：①普攻原版怪 ②fdbosses 调 super 扣血但 return false ③Boss 假成功/拦截）。未扣血时手动 post `LivingHurtEvent` 再经 Accessor 直调 `actuallyHurt()`。**放行时也 postHurtEvent**（fantasy_ending 等 ASM 改 hurt 的模组会跳过原版 ForgeHooks.onLivingHurt，导致 LivingHurtEvent 不 post，影杀等监听器不触发）|
| **Layer 1** | `PiercingGazeMixin`：`Entity.isInvulnerableTo()`（`m_6673_`）@HEAD | 基于 `isInvulnerableTo` 的无敌（原版和大多数模组 Boss）。追溯攻击者（直接实体 -> 弹射物 owner），持有能力则 `setReturnValue(false)` |
| **Layer 2** | `PiercingGazeLivingEntityMixin`：`LivingEntity.hurt()`（`m_6469_`）HEAD + RETURN，**栈式递归隔离** | 调了 `super.hurt()` 但被子类强制 return false 的 Boss；hurt 后偷偷 `setHealth` 回血的 Boss。HEAD（`onHurtEnter`）压栈保存外层状态（4 个 ThreadLocal：`IN_PIERCING` / `PIERCING_EVENT_POSTED` / `PIERCING_EFFECTIVE_AMOUNT` / `PIERCING_STACK`）并重置本层；**v1.3.1 栈深哨兵**：压栈前若栈深 > 64 判定为外部 cancel `hurt()` 导致的帧残留，清空并重置三态，穿透链在下次攻击时自愈；RETURN（`onHurtReturn`）按 A/B/C 三情况补 post 或补 `actuallyHurt`（经 `PiercingGazeLivingEntityAccessor` `m_6475_`），血量没降则 `setAllHealthLikeRaw` 直写扣除，顺带 `InvulClearUtil.clearCustomInvulTimers()`，`finally` 弹栈恢复外层 |
| **Layer 2.5** | 同上类：`@Redirect` 替换 hurt 内部的 `ForgeHooks.onLivingHurt()` 调用 | 在 `LivingHurtEvent` 里 `setAmount()` 压伤害的限伤模组--返回 `max(原始伤害, 事件结果)`，**伤害只能涨不能降**；post 后置 `IN_PIERCING = true` / `PIERCING_EVENT_POSTED = true` / `PIERCING_EFFECTIVE_AMOUNT = effective` 三标记 |
| **Layer 3** | `CombatAbilityHandler.handlePiercingGazeAwakened()`（事件层，`receiveCanceled = true` + `rawAttacker` 回溯） | 觉醒禁无敌帧：目标 `invulnerableTime > 0` 时标记 3 秒（onLivingTick 清零 invulnerableTime）；标记存**内存弱引用表 `WeakHashMap<Entity,Long>`**（实体卸载/GC 即失效、不持久化，排障时勿误判为持久标记） |

- 两处 `@Redirect`（Layer 0 与 Layer 2.5）均带 `require = 0`——注入失败会**静默失效**而非启动崩溃，排查"穿透不生效"时优先检查这两处是否被其他模组抢占

**防事件风暴**（v1.2.4）：第三方模组若在 `LivingHurtEvent` 监听器里再调 `target.hurt()`（破敌之眼源），会形成递归事件风暴。`IN_PIERCING` 风暴守卫检测"是否已在外层破敌之眼穿透内"--redirect 手动 post 后置 `IN_PIERCING = true`，递归 hurt 进入时若发现外层 `IN_PIERCING = true` 则**直接 `return amount`，完全不调 `ForgeHooks.onLivingHurt`**（调它仍会 post 事件触发监听器导致递归，无法防风暴），阻断递归。redirect 与 `onHurtReturn` 两处各有一个风暴守卫分支。

**三种情况 A/B/C**（`onHurtReturn` 区分拦截位置）：
- **A**：Boss 完全拦截（不调 super、return false）--redirect 未执行，`onHurtReturn` 补 post 一次 `LivingHurtEvent` 再经 Accessor 直调 `actuallyHurt`
- **B**：正常流程（super 走完、`actuallyHurt` 执行、return true）--redirect 已 post 一次，`onHurtReturn` 不再 post
- **C**：中途拦截（走到 `onLivingHurt` 后 return false）--redirect 已 post，`onHurtReturn` 复用 `PIERCING_EFFECTIVE_AMOUNT` 补 `actuallyHurt`，不重复 post

- 跨 Mixin 调用 `actuallyHurt` 走 `PiercingGazeLivingEntityAccessor`（`@Invoker`，refmap 映射 `m_6475_`），不做 `(MixinClass)(Object)` 强转
- 所有层都带客户端跳过（防 BetterCombat 客户端预测导致 `ClientLevel->ServerLevel` 强转崩溃）、内部源防重入、友伤保护

配置：`awaken_piercing_gaze_no_iframe_ticks`(60)

### 恩赐永驻 `perpetual_blessing`

**效果**：正面药水效果持续时间低于保底阈值时自动续期，Buff 永不自然耗尽。玩家可在统一面板（P 键 -> Buff永驻标签）把不想续的效果加入黑名单。

**实现**：
- 入口：`PlayerTickDispatcher` 分发的 `PlayerTickHandler.onTick()`，用 `lastBuffCheck` Map 节流为每 60 tick 检查一次
- `extendBeneficialEffects()`：遍历 `player.getActiveEffects()`，只处理 `MobEffectCategory.BENEFICIAL`；效果剩余时间 < `阈值 = BUFF_MIN_DURATION + BUFF_EXTEND_AMOUNT` 时用新 `MobEffectInstance` 覆盖（保留 amplifier/ambient/visible 参数；**v1.3.1 起同时保留 factorData**——潮涌能量等环境效果数据不再因续期丢失）；有续期发生时向附近广播 15 枚 GLOW 粒子反馈
- 黑名单存在 `player.getPersistentData()` 的 `MME_BuffBlacklist` 键下，跨死亡持久；运行时按 UUID 缓存黑名单（每 60 tick 免 NBT 解析），toggle 时失效、登出清理；`onPlayerClone` 手动转移该 key（Forge Clone 不复制根级自定义 key）
- 觉醒：正面效果无法被驱散 -- 监听 `MobEffectEvent.Remove`（实现位于 `AdventureProgressCapability.onBeneficialEffectRemove`，capability 包而非 handler 包），觉醒+恩赐永驻开启时对 `BENEFICIAL` 效果取消移除。覆盖 `removeEffect` / `removeAllEffects` / `cureEffects`(牛奶桶) 三条主动移除路径；自然过期走独立的 `MobEffectEvent.Expired` 不受影响。负面/中性照常可清

**配置**：`buff_min_duration`(400，保底阈值) / `buff_extend_amount`(0，额外延长量；为 0 时阈值即保底值 400 tick)。该能力无成长，解锁即完整。觉醒「无法驱散」无配置项，固定生效。

> v1.3.0 起从里程碑 1 调整至里程碑 9（终末之翼）--前期正面 Buff 少，续期价值有限；后期正面 Buff 多，续期价值更高。

---

## 里程碑 10 · 苍穹之证

### 影杀 `shadow_kill`

**效果**：为攻击目标投影一条"影子血量"（紫色 Boss 栏显示，多人各自独立）。每次攻击削减 `4 + 目标总血量 4%`，与目标真实血量无关--**无论 Boss 怎么回血、锁血、免疫，影子血量只减不增，归零即斩杀**。觉醒后斩杀触发 8 格 AOE，削减周围怪物 15% 影子血量并可连锁斩杀。PVP 无效。

**实现**：
- 入口：`LivingHurtEvent` -> `CombatAbilityHandler.handleShadowKill()`。**v1.2.8**：同 tick 去重（`SHADOW_KILL_TICKED`，防 Layer 0 放行 postHurtEvent + 原版 post 双倍削减）；改签名接收 progress（onLivingHurt 一次取 progress 传各 handle，6 次 Capability 查询 -> 1 次）。**v1.3.1**：去重 key 改为 (attacker, target) 复合——多人同 tick 攻击同一目标各削各的，只防同一攻击者的重复结算。**斩杀归零路径先 `event.setCanceled(true)` 吞掉原事件伤害再走斩杀 hurt，避免双重扣血叠加**
- **影子血量存在攻击者（玩家）侧的 `persistentData`**（`AP_ShadowHP_Data` -> `{目标UUID: {totalHP, shadowHP, endTime}}`）而非目标侧--Boss 无法通过阶段切换、NBT 清理或任何自身手段逃脱。`totalHP` 在首次攻击时快照，防止后续 maxHealth 变化污染计算。**v1.3.1**：维度切换（非死亡 Clone）时数据随玩家转移，跨维度战斗进度保留；死亡清零为刻意设计（重生后重新积累）；玩家死亡重生/换维度时清理其 BossBar（防旧实例重复广播）
- 条目 5 分钟不攻击自动过期（懒清理 + 每 200 tick 全局清理 + 目标死亡/玩家登出清理，含 BossBar 引用防泄漏）。**v1.2.8 新增**：onServerTick 检测 target 有效性（死亡/卸载/换实体后 2 tick 宽限清理 shadowData + BossBar，防 fantasy_ending 等 ASM 改 die 不走 LivingDeathEvent 导致 BossBar 残留）
- BossBar 采用双层 Map（`attackerUUID -> targetUUID -> ServerBossEvent`）：多人同打一 Boss 各有独立 BossBar 显示各自影子血量（数值走翻译键 `ability.adventure_power.shadow_kill.bar` 的"当前/总量"格式），互不干扰；玩家登出时按攻击者维度整体移除

**斩杀链**（`executeShadowKill`，`KILLING` Set 防重入）：
1. 清 `hurtTime`/`invulnerableTime` 后 `target.hurt(killSource, target.getMaxHealth() * 10F)`（**v1.3.1：`killSource` 改为内部伤害类型 `adventure_power:shadow_kill`**——新增 damage_type JSON + 4 个 bypasses 标签，斩杀伤害不再触发嗜血吸血/淬魂追加/影杀递归削减；死亡消息走 `death.attack.shadow_kill` 翻译键；无敌目标现在也能被影杀斩杀。v1.2.4 防溢出：`Float.MAX_VALUE` 在某些模组的 `amount × ratio` 计算中会溢出为 Infinity/NaN 导致卡死，`maxHealth × 10` 足够秒杀且不溢出）
2. 目标还活着（hurt 被完全拦截）-> `saturationKill()` 饱和式处决：
   - `setAllHealthLikeRaw(target, 0)` 直写血量归零
   - 强制掉落装备 + 反射调 `dropAllDeathLoot()`（`m_6668_`）+ 手动 post `LivingDeathEvent`--**战利品不因非常规死亡而丢失**（FTB Quests 等任务模组可正常记录击杀）
   - **五重移除**：`remove(KILLED)` -> `remove(DISCARDED)` -> 反射 `removeDirect` -> 字段直写 `setRemovedFieldDirect` -> `setRemovedDirect(CHANGED_DIMENSION)`（部分 Boss 的 Mixin 只拦 KILLED/DISCARDED，不拦"换维度"）
   - 发 `ClientboundRemoveEntitiesPacket` 强制客户端消失
   - **`eradicateFromWorld()`**：反射从 Minecraft 内部 6 个实体追踪结构直接抹除--`EntityLookup.byId`、`EntityLookup.byUuid`、`PersistentEntitySectionManager.knownUuids`、`EntityTickList`（active/passive）、`EntitySection.classInstanceMultiMap`（allInstances + per-class byClass 分列表）、`ServerChunkCache.removeEntity`。全程 `Field.get` + 集合 `remove()`，不调用任何实体方法，**外部 Mixin 无从拦截**
- 觉醒 AOE（`shadowKillAoe`）：斩杀后对半径 8 格内至多 16 个 `Monster` 各削减 `totalHP × 15%` 影子血量，归零者递归进入斩杀链；**未归零的目标会写入攻击者影子数据新建追踪条目**（新条目按当前 maxHealth 快照，已有条目保留原 totalHP 快照），后续可继续削减——"可连锁斩杀"的可持续性基础

配置：`shadow_kill_flat_damage`(4) / `shadow_kill_hp_ratio`(0.04) / `shadow_kill_data_expire_ticks`(6000) / `awaken_shadow_kill_aoe_radius`(8.0) / `awaken_shadow_kill_aoe_ratio`(0.15) / `awaken_shadow_kill_aoe_max_targets`(16) / `shadow_kill_cleanup_interval`(200)

### 真实血量 `true_health`

**效果**：把玩家血量备份到 Capability，任何绕过正常伤害流程的血量篡改（直写数据、假死标记、强制 die、注入负值偏移）都会被检测并自动修复。

**实现**（`TrueHealthMixin`，5 个注入点组成四层防护）：

| 注入点 | SRG | 职责 |
|--------|-----|------|
| `getHealth()` @HEAD | `m_21223_` | **读取层**：从 Capability 备份返回真实血量，绕过被污染的 SynchedEntityData；检测 DataItem 与备份偏差，非法降血直写则 `setAllHealthLikeRaw` 修复；假死（DataItem ≤ 0 但备份 > 0）也修复 |
| `setHealth()` @RETURN | `m_21153_` | **同步层**：只在合法路径更新备份--`HURT_DEPTH > 0`（hurt 调用链内）或回血时同步；外部直调降血**拒绝同步**。觉醒防秒杀底线：非 hurt 路径写入致死值时强制保留 1HP（正常 hurt 打死不受影响） |
| `isDeadOrDying()` @HEAD | `m_21224_` | 备份 > 0 时强制返回 false，反假死 |
| `die()` @HEAD | `m_6667_` | 备份 > 0 时取消，拦截不经 hurt/setHealth 直接调 `die()` 的秒杀 |
| `tick()` @HEAD | `m_8119_` | **存活性自检**：每 tick 检查 `isRemoved()`/零血/死亡标记但备份 > 0 -> `clearRemovedFlag()` + 修复，最后兜底 |

- 修复动作 `repairHealth()` = `setAllHealthLikeRaw(备份值)` + `clearNegativeFloatDeltas()`（清掉外部注入的负值 Float 偏移条目）
- `IN_ON_GET_HEALTH` ThreadLocal 防修复过程中递归触发 `getHealth`
- **备份初始化**：备份值 ≤ 0 时从当前 DataItem 同步初始化（重生后恢复场景）
- **觉醒容差加倍**：觉醒时偏差判定容忍 `EPSILON × 2`（±0.002 内的微小降血不触发修复，避免与觉醒禁疗等高频小幅度直写打架）
- **事件层兜底**（`TrueHealthHandler`）：`LivingDeathEvent` @HIGH 在备份 > 0 时 cancel——`die()` Mixin 被外部 ASM 绕过时（Mixin 注入点失效）的最后防线，与 CLAUDE.md 架构图对应
- "合法降血"的判定依赖拒绝篡改能力维护的 `HURT_DEPTH` 标记（见下）；**v1.3.1**：`HURT_DEPTH` 由 ServerTickEnd 无条件归零兜底（`HealthUtil.resetHurtDepthPerTick`），外部模组 cancel `hurt()` 导致的深度永久残留不再使保护失效

配置：`true_health_debug_log`(false)

### 拒绝篡改 `reject_manip`

**效果**：拦截外部模组对玩家血量的非法直接修改（不经过伤害流程的 `setHealth` 降血）和 maxHealth 恶意压低。觉醒后把被拦截的降血量按 30% 反弹给最后攻击者。

**实现**（与真实血量分工：本能力拦"写入"，真实血量修"已写入"）：
- **HURT_DEPTH 标记**（`RejectHealthManipMixin`）：`hurt()`（`m_6469_`）@HEAD 时 ThreadLocal 计数 +1，@RETURN 时 -1。由此区分"hurt 内部的合法 setHealth"与"外部直调 setHealth"。服务端主线程单线程，ThreadLocal 天然隔离调用链，无锁无查表。**v1.3.1**：外部模组以低优先级 Mixin cancel `hurt()` 时 RETURN 不执行、深度残留 +1——`HealthUtil.resetHurtDepthPerTick()` 在 ServerTickEnd 无条件归零（hurt 不跨 tick，tick 末深度必为 0 是不变量），泄漏窗口仅为 cancel 发生的同 tick 内
- **setHealth 拦截**（`m_21153_` @HEAD）：NaN/Infinity 直接 cancel；回血放行；`HURT_DEPTH > 0` 放行；其余（外部直调降血）-> `ci.cancel()`
- **maxHealth 底线**（`RejectHealthManipAttributeMixin`，`AttributeInstance.setBaseValue()`（`m_22100_`）@HEAD）：新值 < 6.0（3 颗心）时先 O(1) 查 `ATTR_OWNER` 映射找所有者，未命中（getAttribute 未被调用的早期路径）再 fallback 遍历在线玩家并补登记。是受保护玩家则 cancel
- 共享的 `ATTR_OWNER` Map 放在 `RejectHealthManipUtil` 工具类（Mixin 类禁止非 private static 成员）；登记注入点：`getAttribute`（`m_21051_`）@RETURN 时对冒险者/觉醒玩家登记"AttributeInstance -> 所有者"；`remove()`（`m_142687_`）@HEAD 注入 `cleanupOnRemoval`，实体卸载时从 `ATTR_OWNER` 移除其映射，防内存泄漏
- 觉醒反弹：`(当前血量 - 目标写入值) × 30%` 以魔法伤害反弹给 `getLastHurtByMob()`

配置：`awaken_reject_manip_reflect_ratio`(0.30)

### 旅者之力 `active_skill`

**效果**：解锁两个主动技能（Y 切换 / G 释放，共享 0.5 秒 GCD）：
- **旅者审判**（30 秒 CD）：对周围 6 格敌对生物造成 `10 + (最大生命 + 当前生命) × 3% × 里程碑数` 的范围伤害。觉醒范围 ×1.5（9 格）
- **旅者庇护**（90 秒 CD）：5 秒无敌 + 清负面 + 灭火。觉醒后庇护期间可以 30% 速度移动

**实现**：
- 链路：客户端 `InputHandler`（Y 键切换 / G 键释放，本地预检 GCD/CD 减少无效包）-> **v1.3.1 新增 `SkillSwitchPacket`**（Y 键切换改由服务端持久化并回同步，切换状态不再被任意 sync 覆盖重置；服务端拒绝时也回发同步让客户端乐观更新回滚）-> `ActiveSkillPacket` -> 服务端 `ActiveSkillHandler.handleSkillRelease()` **重新完整校验**（skillIndex 范围、`isFullyUnlocked`、能力启用、GCD、各技能 CD--v1.1.8 加固的防御性自门禁）
- **审判**（`executeJudgment`）：自定义伤害类型 `adventure_power:judgment`；**v1.2.8 改为排除玩家/驯服生物，其余都打**（不再限定 Monster，覆盖史莱姆/幻翼等）；血量判断改用 `getHealthDirect`（防 ASM）；带与淬魂相同的**直写补刀**（实际伤害不足 -> `setAllHealthLikeRaw` 修正，归零强制 `die()`）；补刀时同时重置 `invulnerableTime = 0`、`setLastHurtByMob/Player`、反射置 `deathScore = -1`（防 `die()` 内部重复击杀计分，影杀/计分板排障需知）
- **庇护**（v1.2.1 补全无敌与移动锁）：
  - 写 Capability 的 `sanctuaryInvulEnd`，过期由 `PlayerTickHandler.onTick` 清零并同步
  - `PlayerStateHandler.onSanctuaryDamage`（`LivingAttackEvent` @HIGHEST）在无敌期内 `event.setCanceled(true)`，但 `DamageTypeTags.BYPASSES_INVULNERABILITY`（`/kill`、虚空等）**照常放行**，防止玩家用庇护规避管理员指令或掉出世界
  - **非觉醒**期间移动速度锁 0（完全定住不能走），**觉醒**才允许 `AWAKEN_SANCTUARY_SPEED`（默认 0.3）倍减速移动；**v1.2.8：庇护结束时恢复原始移动速度**（记录 `ORIGINAL_MOVE_SPEED`，不硬编码 0.1，不覆盖其他模组修改）
  - `event.isCanceled()` 前置检查，避免重复拦截其他能力已取消的事件
- 冷却全部存 Capability（NBT 持久化，重登不重置），释放后 `syncToClient` 驱动 HUD（`ActiveSkillHudOverlay`）显示冷却环
- 觉醒联动：死亡抗拒触发时直调 `executeJudgment()`，绕过 CD 和 GCD（免费审判）

配置：`active_skill_judgment_base_damage`(10.0) / `active_skill_judgment_hp_ratio`(0.03) / `active_skill_judgment_radius`(6.0) / `active_skill_judgment_cooldown`(600) / `active_skill_sanctuary_duration`(100) / `active_skill_sanctuary_cooldown`(1800) / `active_skill_gcd`(10) / `awaken_judgment_range_mult`(1.5) / `awaken_sanctuary_speed`(0.3)

---

## 附录：能力 × 实现层速查

| 能力 | 事件层 | Mixin | 反射直写 | 觉醒机制强化 |
|------|:------:|:-----:|:--------:|:------------:|
| 灵巧 | ✅ | | | 数值 ×1.5 |
| 大地之力 | ✅ | | | 数值 ×1.5 + 无视水中/空中挖掘惩罚 |
| 虚空踏步 | ✅（+网络包） | | | 二段跳 -> 御风(疾跑冲刺，联动移速) |
| 休养生息 | ✅ | | ✅ 回血/饱食度 | 每周期 +5 HP |
| 灵魂绑定 | ✅ | | | 经验也保留 |
| 不动如山 | ✅（属性直写） | | | 数值 ×1.5 |
| 加速 | ✅（属性 modifier + 水下药水） | | | 疾跑推开敌对生物 |
| 全视之眼 | ✅ 夜视 + 客户端去雾 | | | 威胁雷达（3D 方位） |
| 无形之手 | ✅（属性直写×2） | | | 数值 ×1.5 |
| 磁吸 | ✅（tick 扫描） | | | 范围扩大 + 吸经验球 |
| 不朽装备 | | ✅ ×2 注入 | | 护甲 +2 值 / 武器 +25% 伤害 |
| 鸿运当头 | ✅ | ✅ +ThreadLocal | | 额外 +2 等级 |
| 经验加成 | ✅（PickupXp） | | | 倍率 ×1.5 |
| 环境免疫 | ✅ | | | 免疫一切无实体来源伤害 |
| 嗜血 | ✅ | | ✅ 回血 | 过量转吸收护盾 |
| 伤害抗性 | ✅ | | | 数值 ×1.5 |
| 禁疗之触 | ✅ | ✅ ×2 注入 | ✅ 钳血 | 禁疗+易伤20% |
| 坚韧之躯 | ✅（属性直写） | | | 数值 ×1.5 |
| 死亡抗拒 | ✅ | ✅ ×2 注入 | ✅ 钳血 | 触发免费审判 |
| 受击坚韧 | ✅ | | | +6 层上限 |
| 净魂 | ✅ 兜底 | ✅ 源头拦截 | | 虚弱光环 |
| 满载而归 | ✅ | ✅ ×4 注入 | | 取最大数量 |
| 翱翔 | ✅ | | | 飞行速度 ×1.5 |
| 淬魂之力 | ✅ | | ✅ 补刀 | ≤20% 血斩杀翻倍 |
| 破敌之眼 | ✅ Layer 3 | ✅ ×4 类 5 层 | ✅ 兜底 | 破无敌+禁无敌帧3秒 |
| 恩赐永驻 | ✅ | | | 正面效果无法被驱散 |
| 影杀 | ✅ | | ✅ 全套 | AOE 斩杀链 |
| 真实血量 | | ✅ ×5 注入 | ✅ 修复 | 防秒杀底线（保留1HP） |
| 拒绝篡改 | | ✅ ×2 类 | | 30% 反弹 |
| 旅者之力 | ✅（+网络包） | | ✅ 补刀 | 审判范围 ×1.5、庇护可减速移动 |

## 附录：Mixin 注入点速查

17 个 Mixin 文件（`src/main/resources/mixins.adventure_power.json` 注册），按目标方法 SRG 名注入（Mixin 编写规范见项目 CLAUDE.md）：

| Mixin 文件 | 目标类 | 注入点 | 服务能力 |
|-----------|--------|--------|----------|
| `PiercingGazePlayerAttackMixin` | `Player` | `attack` `m_5706_` @Redirect 替换内部 `target.hurt()` | 破敌之眼 Layer 0 |
| `PiercingGazeMixin` | `Entity` | `isInvulnerableTo` `m_6673_` @HEAD | 破敌之眼 Layer 1 |
| `PiercingGazeLivingEntityMixin` | `LivingEntity` | `hurt` `m_6469_` @HEAD/@RETURN + @Redirect 替换 `ForgeHooks.onLivingHurt()` | 破敌之眼 Layer 2/2.5 |
| `PiercingGazeLivingEntityAccessor` | `LivingEntity` | @Invoker `actuallyHurt` `m_6475_`（refmap 映射） | 破敌之眼跨 Mixin 调用 |
| `HealingBlockMixin` | `LivingEntity` | `setHealth` `m_21153_` @RETURN + `tick` `m_8119_` @TAIL（priority 2000） | 禁疗之触（跟踪低水位 + 钳制） |
| `TrueHealthMixin` | `LivingEntity` | `getHealth` `m_21223_` HEAD / `setHealth` `m_21153_` RETURN / `isDeadOrDying` `m_21224_` HEAD / `die` `m_6667_` HEAD / `tick` `m_8119_` HEAD（priority 10000） | 真实血量（5 注入点四层防护） |
| `RejectHealthManipMixin` | `LivingEntity` | `hurt` `m_6469_` HEAD/RETURN（HURT_DEPTH 计数）/ `setHealth` `m_21153_` HEAD / `remove` `m_142687_` HEAD（ATTR_OWNER 清理）/ `getAttribute` `m_21051_` RETURN（ATTR_OWNER 登记） | 拒绝篡改 |
| `RejectHealthManipAttributeMixin` | `AttributeInstance` | `setBaseValue` `m_22100_` HEAD（maxHealth 底线 6.0） | 拒绝篡改 |
| `DeathDefyMixin` | `LivingEntity` | `setHealth` `m_21153_` HEAD + `tick` `m_8119_` TAIL（priority 1500） | 死亡抗拒（无敌期拒降血 + 每 tick 拉回） |
| `LegacyHurtAndBreakMixin` | `ItemStack` | `hurtAndBreak` `m_41622_` HEAD + `hurt` `m_220157_` HEAD | 不朽装备（双耐久入口） |
| `PurifiedSoulMixin` | `LivingEntity` | `canBeAffected` `m_7301_` HEAD（priority 2000） | 净魂（源头免疫） |
| `FortuneFavorMixin` | `EnchantmentHelper` | `getItemEnchantmentLevel` `m_44843_` @RETURN | 鸿运当头（时运） |
| `LootPoolEntryContainerMixin` | `LootPoolEntryContainer` | `canRun` `m_79639_` @HEAD | 满载而归（无视 entry 级条件） |
| `LootPoolMixin` | `LootPool` | `addRandomItems` `m_79053_` @HEAD | 满载而归（无视 pool 级条件） |
| `LootPoolAccessor` | `LootPool` | @Accessor `entries`（refmap `f_79023_`） | 满载而归（暴露 entries 字段） |
| `LootContextBuilderMixin` | `LootContext.Builder` | `create` `m_287259_` @ModifyArg（random 参数注入 `ConstantMaxRandomSource`） | 满载而归（觉醒取最大数量） |
| `LootItemConditionalFunctionMixin` | `LootItemConditionalFunction` | `apply` 内 @Redirect `Predicate.test` | 满载而归（无视 function 级条件） |

## 附录：版本演进摘要

散见各章节的演进脚注汇总（按版本）：

- **v1.1.6**：翱翔精准回收（`soarGrantedFlight` 三条件），避免误没收其他模组飞行
- **v1.1.8**：主动技能服务端防御性自门禁加固
- **v1.1.9**：休养生息改为直写方案（`HealthUtil`）
- **v1.2.0**：嗜血改直写回血；v1.2.1：门禁统一里程碑制（移除 5 能力 `isFullyUnlocked()` 硬门禁）、庇护无敌与移动锁补全；v1.2.2~v1.2.3：灵魂绑定死亡优先级调整（@LOW）
- **v1.2.4**：破敌之眼防事件风暴（`IN_PIERCING` 守卫）、影杀斩杀防溢出（`maxHealth × 10`）
- **v1.2.5**：`adventurer` 不变式（全解锁蕴含冒险者）、攻击方能力 `receiveCanceled = true` + `rawAttacker` 回溯、HealthUtil 参照血量/值范围约束
- **v1.2.7**：门禁链封装 `AbilityGate`、成长公式封装基类（`LinearGrowthAbility`/`StepGrowthAbility`）
- **v1.2.8**：影杀同 tick 去重 + 服务端有效性清理、淬魂/审判直写补刀、嗜血满血转护盾修复、破敌 Layer 0 血量比对重构、庇护结束时恢复原速、翱翔空中自动 `flying=true`、禁疗钳制收敛到 Mixin tick TAIL
- **v1.3.0**：伤害抗性 4→6、死亡抗拒 9→7、恩赐永驻 1→9 里程碑调整；虚空踏步重构为单次二段跳 + 觉醒御风（疾跑冲刺）+ 动量物理；全视之眼觉醒威胁雷达
- **v1.3.1**：30+ 处修复（`AbilityGate.isActive`/`value`、`AbilityIds`、`ProgressCache`、成长下限保护、`HURT_DEPTH` tick 末归零、影杀内部伤害类型 `shadow_kill` + 击杀翻译键、影杀去重复合 key、破敌栈深哨兵、护盾/触及/击退原值恢复、`SkillSwitchPacket`、雷达方位词本地化、全视 SEEING 拼写统一、恩赐永驻保留 factorData 等）

## 附录：能力联动关系

- **虚空踏步 × 加速**：御风冲刺冲量随当前移速加成（`dashAmount × 移速/基础移速`），跑得快冲得远
- **淬魂之力 × 禁疗之触**：对禁疗标记中目标伤害 ×`soul_quench_healing_block_multiplier`(1.5)
- **死亡抗拒 × 旅者之力**：觉醒触发时免费释放一次审判（不消耗 CD、不占 GCD）
- **真实血量 × 拒绝篡改**：分工明确——拒绝篡改拦「写入」（`setHealth` 拦截），真实血量修「已写入」（getHealth 时检测修复）；两者共享 `HURT_DEPTH` 标记判定合法伤害
- **破敌之眼 × 影杀/嗜血等监听器**：Layer 0 放行时手动 post `LivingHurtEvent`，保证被 ASM 改 hurt 的模组跳过原版事件时，影杀/嗜血等事件监听器仍能触发
- **嗜血 × 伤害抗性**：嗜血改 @LOW 基于减伤后最终金额吸血，消除与伤害抗性的顺序不确定性
- **影杀/淬魂/审判 × 嗜血**：内部伤害源（`soul_strike`/`judgment`/`shadow_kill`）不触发二次吸血（`isInternalSource`）
- **磁吸 × 经验加成**：磁吸觉醒吸取的经验球同样走原版拾取流程，经验加成补差额照常生效

## 附录：验证与调试指引

- **构建**：`./gradlew build -x test`，构件在 `build/libs/`
- **里程碑解锁验证**：通过 P 键面板「冒险进度」标签查看；服务端计分板 `adventure_power_unlock` 全解锁后为 1，可用命令方块/数据包查询
- **数据包热更新**：修改 `milestones.json` 后执行 `/reload` 即时生效（`MilestoneRegistry` 重新加载，内置兜底 JSON 不参与覆盖）
- **调试开关**：`true_health_debug_log`（真实血量）TOML 配置开启日志
- **性能热点**：`getHealth`/`getAttribute`/`setHealth` 等 MC 高频调用点全部经 `ProgressCache` 按 tick 缓存 progress 引用；磁吸/雷达/虚弱光环等周期行为均有扫描间隔配置（见各能力配置行）

## 附录：文档维护约定

新增/修改能力后，本文档需要同步的位置：

1. 各能力章节的效果/实现/配置行（配置名与默认值必须与 `ModConfig.java` 一致）
2. 「数值成长公式」后的成长类型表（新增有成长能力时）
3. 「觉醒机制」后的觉醒类型速查表
4. 「共享基础设施」——新增共享工具类时补一节
5. 「附录：Mixin 注入点速查」——新增/修改 Mixin 时同步（SRG 名须先在 tsrg 核实）
6. 「附录：版本演进摘要」——版本发布时追加一行
7. 标题版本号（`vX.Y.Z`）与 `gradle.properties` 的 `mod_version` 保持一致

> 本文档基于 v1.3.1 源码编写（含 v1.3.0 全部能力）。配置项完整定义见 `config/ModConfig.java`；里程碑与能力的对应关系可通过数据包 `milestones.json` 自定义（见 `docs/datapack-guide.md`）。
